# Sanity Studio Configuration
# Copy this file to .env.local and fill in your values
# For Vercel deployment, add these as environment variables in the Vercel dashboard

# Required: Your Sanity project ID
SANITY_STUDIO_PROJECT_ID=your-project-id
# Or use NEXT_PUBLIC_SANITY_PROJECT_ID or VITE_SANITY_PROJECT_ID

# Required: Your Sanity dataset name (defaults to "production" if not set)
SANITY_STUDIO_DATASET=production
# Or use NEXT_PUBLIC_SANITY_DATASET or VITE_SANITY_DATASET

# Optional: API token for authenticated requests
SANITY_STUDIO_API_TOKEN=your-api-token-here

